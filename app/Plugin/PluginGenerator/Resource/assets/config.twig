{% extends 'default_frame.twig' %}

{% block title %}[code]画面{% endblock %}
{% block sub_title %}{% endblock %}

{% form_theme form 'Form/bootstrap_3_horizontal_layout.html.twig' %}

{% block main %}
    <div class="row">
        <div class="col-md-12">
            <form class="form-horizontal" method="post" action="{{ url('plugin_[code]_config') }}">
                {{ form_widget(form._token) }}
                <div class="form-group {% if form.name.vars.errors|length %} has-error {% endif %}">
                    <label class="col-sm-4 control-label">項目A<span class="text-danger">*</span></label>
                    <div class="col-sm-8">
                        {{ form_widget(form.name) }}
                        {{ form_errors(form.name) }}
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-offset-3 col-sm-6">
                        <button type="submit" class="btn btn-primary btn-lg btn-block">登録</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
{% endblock main %}
